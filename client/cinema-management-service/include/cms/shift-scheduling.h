#pragma once

#include <cms/cms.h>

#include <stdint.h>
#include <stdbool.h>

/*
struct cms_get_shifts_response { struct cms_response; struct shift_t* shifts; };
extern bool cms_get_shifts(cms_t cms, struct get_shifts_response** response);

struct cms_add_shift_request { int32_t employee_id; char day[CMS_DATE_LEN]; char start_time[CMS_TIME_LEN]; char duration[CMS_TIME_LEN]; int32_t cinema_id; };
extern bool cms_add_shift(cms_t cms, struct add_shift_request request);

struct cms_delete_shift_request { int32_t employee_id; char day[CMS_DATE_LEN]; char start_time[CMS_TIME_LEN]; };
extern bool cms_delete_shift(cms_t cms, struct cms_delete_shift_request request);
*/
